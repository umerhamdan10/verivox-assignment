export declare class UrlJoin {
    private readonly baseUrl;
    private readonly paths;
    private params;
    constructor(baseUrl: string | undefined, paths: string[]);
    toString(): string;
    queryParams(params: object): UrlJoin;
    private hasParams;
}
